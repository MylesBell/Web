deployment:
  staging:
    branch: master
    codedeploy:
      3rd Year Web:
        deployment_group: my-deployment-group
machine:
  node:
    version: 5.1.0
test:
  pre:
    - ./node_modules/grunt-protractor-runner/node_modules/protractor/bin/webdriver-manager update
    - grunt install
    - sudo apt-get upgrade google-chrome-stable --fix-missing

